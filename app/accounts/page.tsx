import type { Metadata } from "next";
import Link from "next/link";
import { PageIntro } from "../components/Sections";
import { accounts } from "../data";
export const metadata: Metadata = { title: "账号运营", description: "腾讯微信读书内容账号与原创剧情账号运营矩阵。" };
export default function AccountsPage() { return <main id="main" className="page-shell"><PageIntro eyebrow="MEDIA ACCOUNTS · 账号运营" title="Accounts I’ve Built"><p>多账号运营不是机械复制内容，而是为不同的人重新理解：她为什么停留、互动、收藏，又为什么愿意持续回来。</p></PageIntro><div className="stats-row"><div><b>6</b><span>腾讯实习期间并行账号</span></div><div><b>4+</b><span>涉及内容赛道</span></div><div><b>2</b><span>独立负责重点项目</span></div><div><b>48万</b><span>代表内容单篇最高曝光</span></div></div><section className="page-content"><div className="account-strip">{accounts.map((account, i) => <Link href={`/accounts/${account.slug}`} className={`account-card tone-${account.tone}`} key={account.slug}><span>ACCOUNT FILE / 0{i + 1}</span><h3>{account.title}</h3><p>{account.source}<br />{account.role}</p><div>{account.metrics.map((metric) => <strong key={metric}>{metric}</strong>)}</div><small>阅读完整策略与复盘 ↗</small></Link>)}</div><div className="table-scroll" aria-label="账号矩阵表格，可横向滚动"><table className="account-matrix"><thead><tr><th>账号 / 项目</th><th>来源</th><th>内容赛道</th><th>我的职责</th><th>代表结果</th></tr></thead><tbody>{accounts.map((account) => <tr key={account.slug}><td><Link href={`/accounts/${account.slug}`}>{account.title}</Link></td><td>{account.source}</td><td>{account.category}</td><td>{account.role}</td><td>{account.metrics.slice(0,2).join(" / ")}</td></tr>)}</tbody></table></div><p className="hand-note" style={{textAlign:"center", marginTop:60}}>不同账号，不只是换一种选题，而是重新理解一次用户。</p></section></main>; }
